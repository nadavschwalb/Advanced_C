Print_Formats - functions that take in the file, buffer, and options flag and prepare a string for printing to stdin
Regex_Engine - engine parses, iterates on and has other utility functions for regex structs
search_str - searches in strings for the correct substring or string relative to regex or non-regex 
